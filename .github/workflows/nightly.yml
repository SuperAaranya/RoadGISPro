name: roadgis-nightly

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build nightly ${{ matrix.label }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Windows
            os: windows-latest
            artifact_name: RoadGISPro-nightly-windows
            artifact_path: installer/windows-exe/out/RoadGISProSetup.exe
          - label: macOS
            os: macos-latest
            artifact_name: RoadGISPro-nightly-macos
            artifact_path: installer/macos-pyinstaller/out/RoadGISPro-macos.zip
          - label: Linux
            os: ubuntu-latest
            artifact_name: RoadGISPro-nightly-linux
            artifact_path: installer/linux-pyinstaller/out/RoadGISPro-linux.tar.gz

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install .[dev,3d]

      - name: Install Windows packaging dependencies
        if: runner.os == 'Windows'
        run: choco install innosetup -y

      - name: Build Windows installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          pwsh .\installer\windows-exe\build_exe.ps1 -Version "1.0.0" -RepoRoot "${{ github.workspace }}"
          $out = "installer\\windows-exe\\out"
          $exe = Get-ChildItem $out -Filter "RoadGISProSetup-*.exe" | Select-Object -First 1
          if (-not $exe) { throw "EXE not found in $out" }
          Copy-Item $exe.FullName "$out\\RoadGISProSetup.exe" -Force

      - name: Build macOS app
        if: runner.os == 'macOS'
        shell: bash
        run: |
          chmod +x ./installer/macos-pyinstaller/build_app.sh
          ./installer/macos-pyinstaller/build_app.sh "nightly"

      - name: Package macOS artifact
        if: runner.os == 'macOS'
        shell: bash
        run: |
          cd installer/macos-pyinstaller/out
          ditto -c -k --sequesterRsrc --keepParent RoadGISPro.app RoadGISPro-macos.zip

      - name: Build Linux app
        if: runner.os == 'Linux'
        shell: bash
        run: |
          chmod +x ./installer/linux-pyinstaller/build_app.sh
          ./installer/linux-pyinstaller/build_app.sh "nightly"

      - name: Package Linux artifact
        if: runner.os == 'Linux'
        shell: bash
        run: |
          cd installer/linux-pyinstaller/out
          tar -czf RoadGISPro-linux.tar.gz RoadGISPro

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          if-no-files-found: error

  publish-nightly:
    name: Publish nightly release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download workflow artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Remove previous nightly assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          for asset in \
            RoadGISProSetup.exe \
            RoadGISPro-macos.zip \
            RoadGISPro-linux.tar.gz
          do
            gh release delete-asset nightly "$asset" --repo "${{ github.repository }}" --yes || true
          done

      - name: Publish nightly release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly
          name: Nightly
          prerelease: true
          body: Automated nightly build from ${{ github.sha }}
          overwrite_files: true
          files: |
            dist/RoadGISPro-nightly-windows/*
            dist/RoadGISPro-nightly-macos/*
            dist/RoadGISPro-nightly-linux/*
